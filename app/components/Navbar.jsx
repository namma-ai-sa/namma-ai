"use client";

export default function Navbar() {
  return (
    <nav
      style={{
        background: "#0f172a",
        color: "#fff",
        padding: "16px",
        borderBottom: "1px solid rgba(255,255,255,.1)",
      }}
    >
      🌱 نمّى AI
    </nav>
  );
}
